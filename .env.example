# =============================================================================
# Feature Request Portal - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Vercel Postgres)
# -----------------------------------------------------------------------------
# Get these from your Vercel project dashboard:
# 1. Go to your Vercel project → Storage → Create Database → Postgres
# 2. Copy the connection strings from the "Quickstart" tab
#
# For local development, you can also use:
# - Docker: docker run -p 5432:5432 -e POSTGRES_PASSWORD=postgres postgres
# - Or use Vercel CLI: vercel env pull .env.local
# -----------------------------------------------------------------------------
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# -----------------------------------------------------------------------------
# LLAMA API (Meta's Public LLM API)
# -----------------------------------------------------------------------------
# Obtain your API key from: https://llama.developer.meta.com
#
# Steps to get your key:
# 1. Visit https://llama.developer.meta.com
# 2. Sign in or create an account
# 3. Navigate to API Keys section
# 4. Generate a new API key
# 5. Copy the key (format: LLM|<app_id>|<secret>)
#
# SECURITY: This key should NEVER be exposed to the client/browser
# All Llama API calls are made server-side only via /api routes
# -----------------------------------------------------------------------------
LLAMA_API_KEY=

# -----------------------------------------------------------------------------
# NEXTAUTH.JS CONFIGURATION
# -----------------------------------------------------------------------------
# NEXTAUTH_SECRET: Generate with: openssl rand -base64 32
# NEXTAUTH_URL: Your app's base URL (http://localhost:3000 for local dev)
# -----------------------------------------------------------------------------
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# GOOGLE OAUTH CREDENTIALS
# -----------------------------------------------------------------------------
# Set up Google OAuth:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Navigate to "APIs & Services" → "Credentials"
# 4. Click "Create Credentials" → "OAuth client ID"
# 5. Select "Web application"
# 6. Add authorized JavaScript origins:
#    - http://localhost:3000 (development)
#    - https://your-app.vercel.app (production)
# 7. Add authorized redirect URIs:
#    - http://localhost:3000/api/auth/callback/google (development)
#    - https://your-app.vercel.app/api/auth/callback/google (production)
# 8. Copy the Client ID and Client Secret
#
# NOTE: Meta employees use @meta.com or @fb.com Google accounts
# Users with these email domains are automatically granted admin access
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
